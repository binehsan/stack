# Stack

A minimal daily to-do dump. See [`assets/requirements.md`](assets/requirements.md)
for the full spec.

- [`backend/`](backend/README.md) — Django + Django REST Framework API (SQLite)
- [`frontend/`](frontend/README.md) — Expo (React Native) app, run via Expo Go

Start with the backend README, then the frontend README — the frontend needs
the backend running and reachable on your local network to load or save tasks.
