// The link the "Get the app" QR code and buttons point to. Change this the
// moment real App Store / Google Play listings exist. Override per-deploy
// with VITE_MOBILE_APP_LINK without touching code; falls back to a
// placeholder otherwise.
export const MOBILE_APP_LINK = import.meta.env.VITE_MOBILE_APP_LINK || 'https://stackapp.example/download';
